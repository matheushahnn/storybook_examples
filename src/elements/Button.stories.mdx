import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import Button from './Button';

<Meta title="Button" />

# Buttons

With `MDX` we can define a story for `Button` right in the middle of our
markdown documentation.

This is the enable button
<Preview>
  <Story name="button">
    <div style={{ width: 200 }}>
      <Button label="Submit" />
    </div>
  </Story>
</Preview>

This is another disabled button

<Preview>
  <Story name="button disabled">
    <div style={{ width: 200 }}>
      <Button label="Submit" isDisabled="true"/>
    </div>
  </Story>
</Preview>
