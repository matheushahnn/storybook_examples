import React from 'react';

describe("Button", () => {  
  it("# should mount", () => {
    expect(true).toEqual(true)
  })

  it("# should not mount", () => {
    expect(true).toEqual(false)
  })
});